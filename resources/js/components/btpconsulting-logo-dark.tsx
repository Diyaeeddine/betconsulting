import Logo from '@/assets/btpconsulting_logo_dark.png';

export default function BtpconsultingDarkLogo({ className }: { className?: string }) {
    return <img src={Logo} alt="Betconsulting Logo" className={className} />;
}
